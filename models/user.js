const user = {
    id: String,
    email: String,
    password: String,
    createdAt: Date
};

export function createUser(user) {
    return user;
}

export function getUser(id) {
    return user;
}

export default user; 